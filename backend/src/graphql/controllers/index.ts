import * as personController from "./personController";
import * as postController from "./postController";
import * as segmentController from "./segmentController";

// All controllers
const controllers = {
  personController,
  postController,
  segmentController,
};

export default controllers;
